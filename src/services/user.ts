import { Request, Response } from "express";

class User {
  //   getInfoFromSession = async (req: Request, res: Response) => {
  //     const user = req.session.user;
  //     return user;
  //   };
}

export const user = new User();
